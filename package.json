{
  "name": "rational-use",
  "version": "0.1.0",
  "description": "Orchestrate deterministic scripts and LLM prompts from one recipe. Spend tokens only where judgment is actually needed.",
  "type": "module",
  "bin": {
    "ruse": "src/cli.mjs"
  },
  "exports": {
    "./kit": "./src/runtime.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "example": "node src/cli.mjs run examples/summarize.recipe.mjs",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "echo \"No linting yet\" && exit 0",
    "translations:sort:fix": "echo \"No translations yet\" && exit 0",
    "check-types": "echo \"No types yet\" && exit 0",
    "format:write": "echo \"No formatting yet\" && exit 0"
  },
  "license": "MIT"
}

Build a compact map of the codebase from the file listing in the input.
Group files by rough purpose (entry point, runtime, examples, docs, etc.) and
name the single most important file per group. Be terse — this becomes shared
context reused across several later prompts, so brevity matters.
Return only the requested JSON.
